package com.fpsmonitor.app.ui

import android.content.Context
import android.graphics.*
import android.util.AttributeSet
import android.view.View

class FpsWaveformView @JvmOverloads constructor(
    context: Context,
    attrs: AttributeSet? = null,
    defStyleAttr: Int = 0
) : View(context, attrs, defStyleAttr) {

    private val maxPoints = 80
    private val dataPoints = ArrayList<Float>(maxPoints)

    private val linePaint = Paint(Paint.ANTI_ALIAS_FLAG).apply {
        style = Paint.Style.STROKE
        strokeWidth = 3f
        strokeCap = Paint.Cap.ROUND
        strokeJoin = Paint.Join.ROUND
        color = Color.parseColor("#00E676")
    }

    private val fillPaint = Paint(Paint.ANTI_ALIAS_FLAG).apply {
        style = Paint.Style.FILL
    }

    private val gridPaint = Paint(Paint.ANTI_ALIAS_FLAG).apply {
        style = Paint.Style.STROKE
        strokeWidth = 1f
        color = Color.parseColor("#25FFFFFF")
        pathEffect = DashPathEffect(floatArrayOf(5f, 5f), 0f)
    }

    private val textPaint = Paint(Paint.ANTI_ALIAS_FLAG).apply {
        textSize = 20f
        color = Color.parseColor("#64748B")
        typeface = Typeface.DEFAULT_BOLD
    }

    private val path = Path()
    private val fillPath = Path()

    init {
        for (i in 0 until maxPoints) dataPoints.add(60f)
    }

    fun addDataPoint(value: Float) = addFpsPoint(value)

    fun addFpsPoint(fps: Float) {
        synchronized(dataPoints) {
            if (dataPoints.size >= maxPoints) dataPoints.removeAt(0)
            dataPoints.add(fps.coerceIn(0f, 150f))
        }
        postInvalidateOnAnimation()
    }

    override fun onDraw(canvas: Canvas) {
        super.onDraw(canvas)
        val w = width.toFloat()
        val h = height.toFloat()
        if (w <= 0 || h <= 0) return

        val y60 = h * (1f - 60f / 144f)
        val y120 = h * (1f - 120f / 144f)
        canvas.drawLine(0f, y60, w, y60, gridPaint)
        canvas.drawLine(0f, y120, w, y120, gridPaint)
        canvas.drawText("60", 6f, y60 - 3f, textPaint)
        canvas.drawText("120", 6f, y120 - 3f, textPaint)

        path.reset()
        fillPath.reset()

        val points: List<Float>
        synchronized(dataPoints) { points = ArrayList(dataPoints) }
        if (points.size < 2) return

        val stepX = w / (maxPoints - 1).toFloat()
        var latestFps = 60f

        for (i in points.indices) {
            val fps = points[i]
            val x = i * stepX
            val y = (h - (fps / 144f * (h - 8f))).coerceIn(4f, h - 4f)

            if (i == 0) {
                path.moveTo(x, y)
                fillPath.moveTo(x, h)
                fillPath.lineTo(x, y)
            } else {
                val prevFps = points[i - 1]
                val prevX = (i - 1) * stepX
                val prevY = (h - (prevFps / 144f * (h - 8f))).coerceIn(4f, h - 4f)
                val cX = (prevX + x) / 2f
                path.cubicTo(cX, prevY, cX, y, x, y)
                fillPath.cubicTo(cX, prevY, cX, y, x, y)
            }
            if (i == points.size - 1) {
                latestFps = fps
                fillPath.lineTo(x, h)
                fillPath.close()
            }
        }

        val mainColor = when {
            latestFps >= 55f -> Color.parseColor("#00E676")
            latestFps >= 40f -> Color.parseColor("#FFD600")
            latestFps > 0f -> Color.parseColor("#FF1744")
            else -> Color.parseColor("#94A3B8")
        }
        linePaint.color = mainColor
        fillPaint.shader = LinearGradient(0f, 0f, 0f, h, mainColor and 0x40FFFFFF, Color.TRANSPARENT, Shader.TileMode.CLAMP)

        canvas.drawPath(fillPath, fillPaint)
        canvas.drawPath(path, linePaint)
    }
}
